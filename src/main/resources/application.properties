# Production site URL (used for absolute links, RSS, sitemap)
site.url=http://localhost:8080
%prod.site.url=https://blog.lunatech.com

# Blog posts collection -> rendered with the base theme "post" layout (overridden locally
# by templates/layouts/post.html, which pins the URL pattern for SEO stability).
site.collections.posts.layout=post

# Do not publish future-dated posts until their date passes
site.future=false

# Tagging plugin: lowercase tag keys so /tags/:tag URLs match the theme's slugified links
quarkus.roq.tagging.lowercase=true

# Author bylines: resolve GitHub handles to profile names at startup, before the
# generator renders pages. CI passes the workflow token; unauthenticated local
# builds are rate limited (60/h) and fall back to the handle. Disabled in tests
# so they stay hermetic.
github.token=${GITHUB_TOKEN:}
blog.author-lookup.enabled=true
%test.blog.author-lookup.enabled=false
quarkus.rest-client.github.url=https://api.github.com
quarkus.rest-client.github.connect-timeout=10000
quarkus.rest-client.github.read-timeout=10000
